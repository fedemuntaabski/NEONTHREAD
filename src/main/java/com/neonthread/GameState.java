package com.neonthread;

/**
 * Estados del juego siguiendo patrón State Machine simple.
 */
public enum GameState {
    STATE_BOOTSTRAP,
    STATE_BOOT,
    STATE_LOGO_GLITCH,
    STATE_TITLE,
    STATE_MENU
}
